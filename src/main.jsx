import React from 'react'
import ReactDOM from 'react-dom/client'
import { PickerColorPage } from './pages/PickerColorPage'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PickerColorPage />
  </React.StrictMode>
)
